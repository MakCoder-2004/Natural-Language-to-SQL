# Evaluation Datasets

Benchmark questions and expected concepts belong here. Representative schemas
and data remain isolated under `tests/fixtures/`.

`nl2sql_mvp_v1.json` contains the versioned 50-case MVP dataset. Each case labels
the expected classification, source tables, concepts, and relationships without
embedding business rows in the evaluation artifact.
